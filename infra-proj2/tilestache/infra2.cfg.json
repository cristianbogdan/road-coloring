{
    "cache": {
	"name": "Multi",
	"tiers": [
            {
		"name": "Disk",
		"path": "/tmp/stache",
		"dirs":"portable",
		"gzip": []
            }
	]
    },
    "layers": {
	"infraGraphic":
        {
    	    "provider":
            {		  
		"name": "mapnik",
		"mapfile": "projects-mapnik-noent.xml"
	    }
        }
	
	,
	"infraVector":{ 
	    "allowed origin": "*", 
    	    "provider":{     
		"class":"TileStache.Goodies.VecTiles:Provider",
		"kwargs":{
            	    "dbinfo":{
			"user": "postgres",
			"database": "gis1"
            	    },
            	    "queries":[
			null, null, null, null, null,
        		null, null,
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1 where not(highway in ('motorway_link', 'trunk_link' ))",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1 where not(highway in ('motorway_link', 'trunk_link' ))",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1 where not(highway in ('motorway_link', 'trunk_link' ))",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1 where not(highway in ('motorway_link', 'trunk_link' ))",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1",
			"SELECT osm_id, way AS __geometry__, status, ref, name, highway FROM planet_osm_line1"
		    ]
		}   
	    }
	}
    }
}
